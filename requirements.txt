# Core (pin EXACT versions after first install: pip freeze > requirements.lock)
torch>=2.4
transformers>=4.46
tokenizers>=0.20
accelerate>=1.0
bitsandbytes>=0.44
datasets>=3.0
peft>=0.13
numpy>=1.26
pyyaml>=6.0
# Week 2+: trl, vllm, wandb, datasketch, lm-eval -- add when first used,
# not before (smaller env = fewer resolution conflicts on rented pods).
requests